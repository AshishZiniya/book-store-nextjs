import React from 'react';
import CartClient from '@/components/CartClient';

export default function CartPage() {
  return <CartClient />;
}

export const metadata = {
  title: 'Shopping Cart | BookStore',
  description: 'Review and manage your book selections',
};
